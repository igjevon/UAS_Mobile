<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="umn.ac.id.fashop">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".ProductDetailsOutersActivity"></activity>
        <activity android:name=".ProductDetailsDressesActivity" />
        <activity android:name=".ProductDetailsBottomsActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".Outers_Activity" />
        <activity android:name=".Profile1" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".MyHistoryDetailActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".LoungeWears_Activity" />
        <activity android:name=".Dresses_Activity" />
        <activity android:name=".Bottoms_Activity" />
        <activity android:name=".Tops_Activity" />
        <activity android:name=".EditProfileAct" />
        <activity android:name=".SuccessBuyActivity" />
        <activity android:name=".ListProductActivity" />
        <activity android:name=".ProductDetailsActivity" />
        <activity android:name=".CheckoutActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SuccessRegisterAct" />
        <activity android:name=".RegisterTwoAct" />
        <activity android:name=".RegisterOneAct" />
        <activity android:name=".SigninAct" />
        <activity android:name=".GetStartedAct" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.WAKE_LOCK" />
</manifest>